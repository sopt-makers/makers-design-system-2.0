# Getting Started

이 페이지는 **MDX 툴체인**이 정상 동작하는지 확인하기 위한 샘플 문서입니다.

- Vite + React Router + MDX 파이프라인이 연결되어 있습니다.
- 이후 실제 문서(Color System 등)는 이 형식의 `.mdx`로 작성합니다.

```tsx
import { ActionButton } from "@sopt-mds/ui";
```

> 위 import는 버튼 머지 이후 사용 가능합니다.
