{
  "name": "makers-design-system",
  "private": true,
  "scripts": {
    "ci:publish": "pnpm publish -r --report-summary",
    "changeset": "changeset",
    "changeset:status": "changeset status --verbose",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "release": "pnpm ci:publish",
    "versioning": "changeset version && pnpm install --no-frozen-lockfile"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@changesets/cli": "^2.27.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@10.26.2",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  }
}
