{
  "name": "makers-design-system",
  "private": true,
  "scripts": {
    "changeset": "changeset",
    "changeset:status": "changeset status --verbose",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "release": "changeset publish",
    "versioning": "changeset version && pnpm install --lockfile-only",
    "storybook": "pnpm --filter @sopt-mds/storybook dev",
    "storybook:build": "pnpm --filter @sopt-mds/storybook build",
    "web": "pnpm --filter @sopt-mds/web dev",
    "web:build": "pnpm --filter @sopt-mds/web build",
    "typecheck": "pnpm -r typecheck && pnpm typecheck:scripts",
    "typecheck:scripts": "tsc --noEmit -p scripts/tsconfig.json",
    "scaffold:component": "tsx scripts/scaffold-component.ts",
    "tokens:sync": "tsx scripts/sync-design-tokens/index.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@changesets/cli": "^2.27.0",
    "@inquirer/prompts": "^7.2.0",
    "@types/node": "^22.19.17",
    "es-toolkit": "^1.50.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@10.26.2",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  }
}
